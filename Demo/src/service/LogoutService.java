package service;

import model.User;

public class LogoutService {

    // TODO: Sinh viên tự code logic: Thực hiện các bước xóa session người dùng hiện tại, dọn dẹp các cache tạm thời nếu có để đảm bảo an toàn thông tin. Trả về true nếu đăng xuất thành công.
    public boolean performLogout(User currentUser) {
        return false;
    }
}
