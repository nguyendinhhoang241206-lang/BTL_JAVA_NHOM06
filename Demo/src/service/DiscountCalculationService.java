package service;

public class DiscountCalculationService {

    // TODO: Sinh viên tự code logic: Nhận mã giảm giá (promoCode) và số tiền ban đầu (originalPrice). So khớp promoCode với các quy tắc/mã hệ thống (ví dụ: "KM10" -> giảm 10%, "GIAM50K" -> giảm 50.000đ). Tính toán số tiền được giảm giá và trả về.
    public double calculateDiscount(String promoCode, double originalPrice) {
        return 0.0;
    }

    // TODO: Sinh viên tự code logic: Trừ số tiền giảm giá (discountAmount) khỏi tổng tiền tạm tính ban đầu (originalPrice). Đảm bảo số tiền sau khi giảm không nhỏ hơn 0. Trả về tổng tiền cuối cùng phải thanh toán.
    public double calculateFinalPrice(double originalPrice, double discountAmount) {
        return 0.0;
    }
}
