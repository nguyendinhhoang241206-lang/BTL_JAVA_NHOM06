package controller;

import model.Movie;
import service.EditMovieService;

public class EditMovieController {
    private EditMovieService editMovieService = new EditMovieService();

    // TODO: Sinh viên tự code logic: Nhận thông tin phim đã chỉnh sửa từ View (Form sửa phim), validate dữ liệu đầu vào và gọi editMovieService.editMovie(movie). Trả về kết quả cho giao diện người dùng.
    public boolean handleEditMovie(Movie movie) {
        return false;
    }
}
