package controller;

import model.User;
import service.LoginService;

public class LoginController {
    private LoginService loginService = new LoginService();

    // TODO: Sinh viên tự code logic: Nhận đầu vào username, password từ giao diện Đăng nhập, kiểm tra validation cơ bản, gọi loginService để xác thực và trả kết quả phản hồi về giao diện.
    public boolean handleLogin(String username, String password) {
        return false;
    }
}
